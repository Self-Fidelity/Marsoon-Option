# Go options service, server-side only. Unified mode requires the new backend.
OPTIONS_API_BASE_URL=http://127.0.0.1:8001
OPTIONS_API_MODE=unified
# For an older deployed backend, set OPTIONS_API_MODE=legacy.
# OPTIONS_API_TOKEN=
