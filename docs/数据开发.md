# 数据开发

> 上游仓库：本仓 Marsoon-Option（Next.js 16 + React 19 + TS + Tailwind 4 + TanStack Query），本文档位于仓内 `docs/`。
> 本文梳理：现有接口、口径红线、面板 × 数据映射、缺口清单、补齐建议。
> 相关文档：`技术架构.md`（分层与契约）、`业务逻辑.md`（面板业务定义与 GEX 口径）。
> 对外版缺口清单：`供应商数据需求.md`（提交数据供应商的正式需求规格，由本文 §4–§6 转化）。

## 1. 现状：数据从哪来

**2026-09-02 更新：真实数据链路已落通并运行中。** Barchart 浏览器态采集器（`scripts/barchart-poller.mjs`，15 分钟/轮）推送快照到 `POST /api/ingest/barchart`，写入内存仓（`src/server/barchart-store.ts`）；dashboard / levels / chain 三个 Route Handler 优先消费真实快照（`source: barchart-delayed`，Black-76 引擎实时计算 Greeks/GEX/墙/翻转位），无快照时回退 `local-demo`（页面顶部显示 DEMO）。

数据正确性已知问题（serie 到期日归属错误、到期覆盖不全等）见 `免费数据源与抓取策略.md` §6 与 `ITERATION.md` P0。

它的分层已经是正确的，我们接入时**只换数据源，不动图表组件**：

```
API Response → TS api types → ViewModel → TanStack Query → 展示组件
```

- 产品：NQ / ES / GC（期货期权，乘数 20/50/100，tick 0.25/0.25/0.1）
- 范围：scope = `close / 0dte / d30 / d90 / all`（close=收盘固定层空位，Databento EOD 待接入；旧值 nearest 已删除；all 显示名 RTH），状态同步 URL
- 时间约定：Unix 秒；`from/to` 为 `[from, to)`；levels timeframe=60s（**1 分钟快照**）、回看 60 分钟 = 60 个桶

## 2. 接口清单

### 已实现（真实数据已接入，无快照时回退 local-demo）

| 接口 | 内容 | 关键字段 |
|---|---|---|
| `GET /api/options/dashboard?product&scope&days=20&window_pct=0.12` | 单快照总览 | `market_state`(spot/regime/net_gex)、`summary`(call_wall/put_wall/gamma_flip/key_gamma/expected_move/atm_iv/PCR_oi)、`expiries[]`(分到期 OI/GEX/atm_iv/expected_move/charm)、`heatmap.cells[]`(expiration×strike 的 call/put GEX、OI、IV) |
| `GET /api/options/levels?product&scope&from&to&timeframe` | 时间序列 | `states[]`(net_gex 等分时)、`levels[]`(call_wall/put_wall/gamma_flip 的迁移点，含 rank)。快照落盘（`data/snapshots/YYYY-MM-DD.jsonl`）与回放已实装：日内轨迹由 `GET /api/options/intraday` 回放 jsonl 提供 |
| `GET /api/options/chain?product&expiration?` | 单 serie 全档明细（09 面板） | 逐档 IV/Δ/Γ/GEX/OI/Vol/bid/ask/last + call_wall/put_wall/gamma_flip/key_gamma/max_pain/expected_move；无快照返回 404（面板不展示模拟数据） |
| `POST /api/ingest/barchart` / `GET`（状态） | 采集器推送快照 / 抓取状态（倒计时徽标轮询） | 快照契约见 `src/server/barchart-store.ts` |

### 已规划（README 提示词中出现，后端待提供）

| 接口 | 用途 |
|---|---|
| `/options/heatmap` | Gamma 地图（dashboard 内 heatmap 的独立版） |
| `/options/0dte-volume-profile` | 0DTE 成交量分布（Call/Put × Buy/Sell/Unknown） |
| `/options/0dte-oi-profile` | 0DTE OI 分布（最新快照） |
| `/options/hedge-flow` | 资金流（net_delta_notional、累计值） |
| level-events | Wall 迁移事件标记 |

## 3. 口径红线（来自上游 README，必须遵守）

- `put_gex` 已带负号：`net_gex = call_gex + put_gex`，**不得重复反号**。
- Unknown 成交量计入总量，但不进方向 Delta。
- OI 是快照，不能跨分钟累加。
- `quality_flags` 是位掩码，不是枚举。
- Wall / Gamma Flip / IV 缺失时**不绘制**，不能降级为 0。
- Heatmap 各 strike 可能来自不同时间，不是原子快照（看 `observed_min/max_unix`，处理 mixed/stale）。
- 后端没给的指标（IV Rank、Max Pain、ΔOI、25Δ Skew、VRP、Pinning）**隐藏或显示 `--`，前端不得自造公式**。

## 4. 面板 × 数据映射与缺口（2026-09-02 更新）

| # | 面板 | 数据来源 | 状态 |
|---|---|---|---|
| 01 | 总览 | dashboard.summary（regime/net_gex/walls/flip/expected move） | ✅ 已实装（真实数据） |
| 05 | 到期热力图 | heatmap.cells（expiration×strike GEX 矩阵） | ✅ 已实装；行轴 25 档（锚点档必含）+ 窗外锚点贴边标记 + 每列独立 CW/PW/FLIP 标记（第三十四轮） |
| 06 | 日内变化 | levels（迁移序列）+ K线 | ✅ 已实装（数据经 intraday 回放 jsonl 提供） |
| 07 | 微笑/偏斜 | cells 的 call_iv/put_iv by strike + expiries.atm_iv | ✅ 已实装 |
| 08 | Call/Put GEX 拆分 | cells（单到期的 call_gex/put_gex） | ✅ 已实装；⚠️ 同 05 的窗口裁剪丢墙问题 |
| 09 | 期权链下钻 | `/api/options/chain`（单 serie 全档 IV/Δ/OI/Vol/bid/ask + Max Pain） | ✅ 已实装（2026-09-02） |
| 10 | 月间价差 · PCR | IV 期限结构 + PCR 曲线 | ✅ 已实装；BCR 槽位占位待付费源 |
| — | Pin Risk / Max Pain | 09 面板已有 Max Pain（自算，OI 加权）；Pin Risk 无字段 | 🚫 Pin Risk 不画，等后端 |

另：Net GEX `Δ5m/Δ15m/Δ30m` 指标已于 2026-09-02 删除（细节指标不做；15 分钟采集周期下 Δ5m 无意义）。

## 5. Intraday 的 K线决策（已闭合，2026-09-02）

**只要简单 K线，不要深度数据**——不存 tick、不存逐笔、不存盘口，省存储也省性能。

- 粒度：**1 分钟 OHLCV**（与快照对齐，方便叠加；前端显示可聚合为 5m/15m）。期货近 24h 约 1500 根/品种/天，三个品种一天数千行，存储仍可忽略。
- **数据源已落实**：Barchart `queryminutes.ashx?symbol=<FUT>&interval=1`（CSV），随每轮快照推送 `bars1m`（保留最近 800 根 ≈13 小时；15 分钟采集间隔不断档）。已知问题：GC 间歇性空窗（采集器静默吞错，ITERATION.md P0-3）。
- 用法（即截图形态）：K线为主图，把 dashboard.summary 的 `call_wall / put_wall / gamma_flip` 画成三条水平线（CW 用 Positive 青绿、PW 用 Negative 红、Flip 用 Brand 金——色值按 `业务逻辑.md` §5 的 Marsoon 主题映射），价格触碰墙位一目了然。
- 注意区分：**K线只描述价格行为**；「GEX 曲线日内演变」由 levels 序列承担（依赖快照落盘）。面板叙事"曲线是记录不是预测"对应这两个数据的分工。

建议接口形态（暂未实现，当前 bars1m 直接随快照存内存仓）：

```http
GET /options/underlying-bars?product=ES&timeframe=60&from=<unix>&to=<unix>
```

```ts
interface UnderlyingBarsResponse {
  product: OptionProduct;
  timeframe: number;                 // 秒，默认 60
  bars: { unix: number; open: number; high: number; low: number; close: number; volume: number }[];
}
```

## 6. 缺口补齐清单（按优先级，2026-09-02 更新）

1. ~~**K线接口**（§5）~~ ✅ 已由 barchart `queryminutes.ashx` 闭合（bars1m 随快照推送）。
2. ~~**09 链下钻补字段**~~ ✅ `/api/options/chain` 已实装（per-strike volume/delta/bid/ask 全有，Max Pain 自算）。
3. **serie 到期日真值与到期覆盖**（新，最高优先）：见 `免费数据源与抓取策略.md` §6——到期日改页面真值；Week/Month 选择器符号全量纳入。
4. **levels 历史**：快照落盘（`data/snapshots/*.jsonl`），迁移序列与日内变化才有真值。
5. **10 的全期限 volume**：把 0dte-volume-profile 推广为按 scope 过滤的 volume profile，成交量 PCR/BCR 才能算。0DTE 限定版可先支撑 0DTE 视角。
6. **Pin Risk**：当前无字段且禁止伪造（Max Pain 已在 09 自算落地）。若后续要做，需要后端正式提供，前端只做展示。
7. **hedge-flow / level-events**：已规划未实现，对应资金流模块，超出当前 10 面板范围，列为后续扩展。

## 7. 与我们工程的对接

- 上游的 `OptionProduct/OptionScope/StrikeHeatmapCell/...` 类型就是我们 `技术架构.md` §5 数据契约的现实来源：迁移时以 `src/api/options.ts` 为底本，`ChainSnapshot` 由 dashboard 的 cells + summary 组装，`GexLandmarks` 直接取 summary 的 walls/flip。
- 看板**实时接后端**：TanStack Query hooks 指向 `/options/*`，盘中 60s 轮询（节奏见 `技术架构.md` §3.3）；无后端联调时用本地 route handler 出 demo 数据（同 Marsoon 的 `local-demo` 做法）。现行口径：真实数据路径为快照版本驱动失效（barchart-delayed 源，非定时轮询；仅 local-demo 回退保留 60s 轮询）。
- 本地开发联调：`pnpm dev`（Node 20.9+ / pnpm 11+，默认 4173 端口）；提交前 `pnpm lint && pnpm build`。
