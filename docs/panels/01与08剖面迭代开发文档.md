# 01 与 08 期权剖面迭代（2026-09-05）

## 01 总览

底部三个迷你柱图合并为 Rust 终端式 Call/Put GEX 双向价位剖面。行权价居中，Put GEX 向左、Call GEX 向右，并叠加 SPOT、Call Wall、Put Wall、Gamma Flip。GEX 数据直接来自 dashboard view model，不重新计算。

## 08 Call / Put GEX 拆分

保留原 Call/Put GEX 剖面和全部价位/追踪/下钻行为，在右侧新增 Net Delta Profile。Delta 使用后端 `net_delta_notional = call_delta_notional + put_delta_notional`；正值向右、负值向左。逐行数字移除，精确值放在 SVG 悬停说明中；字段缺失时该行 Delta 留空。
