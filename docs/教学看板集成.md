# 教学看板集成

## 2026-09-08：Gamma 教学页面进入 `/teaching`

- `/teaching` 使用 Next.js/React 导航壳，默认加载 `首页.html`。
- 05–10 六个教学模块作为自包含 React 构建产物存于 `public/teaching-content/`，保留原动画、键盘、滚轮、圆点和面板内跳转。
- 顶部二级导航提供首页、05 热力图、06 日内、07 波动率、08 GEX、09 期权链、10 月间价差入口。
- URL 使用 `/teaching?lesson=05` 等可分享状态；非法或缺失 lesson 回退首页。
- iframe 内原有相对链接仍在同目录工作；跳转到另一教学模块后，外层 Next URL 与导航选中态在 load 时同步。
- 教学模块不请求期权数据接口，不修改 `/board` 数据链路。
